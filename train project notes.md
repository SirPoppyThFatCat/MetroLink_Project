# train project notes* Task 1  * use a scanner class to read the CSV file into an array list  * each list has 3 elements, using a values.\[] piece of code to go through each one    * arrayList.add()    *   * check is done with another scanner - plus an if statement plus .contains() and while loops. while <isCorrect> is false?    * &#x20;for (int i = 0; i < cars.size(); i++)    * or    * for (String i : cars) {    * &#x20;     System.out.println(i);    * https://www.javaspring.net/blog/java-how-can-i-write-my-arraylist-to-a-file-and-read-load-that-file-to-the-original-arraylist/    * &#x20;       scanner.useDelimiter(",");    * https://stackoverflow.com/questions/14274259/read-csv-with-scanner* task 3  * fucking graphs  * plan ahead for task 4    * create options of graphs that will have minimum spanning trees  * Basic operations = node and edge additions/removals, and adjacency queries:  * • void addNode(Node 𝑛𝑛)  * • void removeNode(Node n)   * • void addEdge(Node n, Node m)  * • void removeEdge(Node n, Node m)  * • boolean adjacent(Node n, Node m)  * • Node\[] getNeighbours(Node n)  * Use all of these to figure out the best way. djikstra's algo?  * Either use a list-based or matrix-based implementation    * is a sparse network with very few edges between each station      * matrix-based has a 99^2 grid - 9801 which uses hella memory    * A list says:      * “For each station, store only the stations it actually connects to.”      * So each station has a list of neighbours.      * extremely useful for a sparse network    * also memory efficient    * dji's algo becomes a lot faster     * easier to build  * USE AN ADJACENCY LIST!!!!